<%= form_for(@network2) do |f| %>
  <% if @network2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@network2.errors.count, "error") %> prohibited this network2 from being saved:</h2>

      <ul>
      <% @network2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :networkee_id %><br>
    <%= f.number_field :networkee_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
